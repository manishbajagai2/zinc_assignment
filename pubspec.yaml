name: zinc_assignment
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  http: ^1.2.1
  provider: ^6.1.2
  cached_network_image: ^3.3.1
  flutter_launcher_icons: ^0.13.1
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter_launcher_icons:
  image_path: "./assets/logo.png"
  android: true
  ios: true

flutter:
  uses-material-design: true
